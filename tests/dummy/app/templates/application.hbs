<p>The Spreadsheet responds to width of browser, so if you shrink browser width, you will see
the horizontal scroll
</p>
<div class="sample sheet one">
  {{#ember-spreadsheet
    itemHeight=itemHeight
    leftWidth=leftWidth

    leftHeaderComponent=leftHeaderComponent
    leftItems=leftItems

    rightHeaderComponent=rightHeaderComponent
    rightItems=rightItems
    rightSideComponent=rightSideComponent
  as |hash|}}
    {{#if hash.isLeftSideItem}}
      {{left-row item=hash.item}}
    {{/if}}
  {{/ember-spreadsheet}}
</div>

